services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: konsultan-bank-web
    ports:
      - "5173:5173"
    command: npm run dev -- --host 0.0.0.0 --port 5173
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
      - /app/node_modules
